# LaevatainWPFExtensions

基于原生 `WPF` 框架开发的拓展，目前仅作为 Windows 桌面端应用开发的学习与尝试，更偏向于通过 C# 端控制组件，初次尝试该方向，若有术语使用错误或不准确请见谅。

已实现：

- ComboBox Extension：
  利用 `ComboBoxPair` 类代理 ComboBox 填充项。
  支持逐 Pair 手动构造、通过自定义命名规则与 `各依赖实例/依赖实例列表` 传入生成；支持 `枚举类型` 的自动生成、自定义命名规则生成。

